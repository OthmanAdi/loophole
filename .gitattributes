# Normalize line endings to LF in the repo so CI on Linux stays deterministic.
* text=auto eol=lf

# Binary assets: never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ablx binary
*.tgz binary
*.wav binary
