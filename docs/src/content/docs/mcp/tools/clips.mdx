---
title: Clip tools
description: List the clips on a track and create an empty MIDI clip in a session slot.
---

import ToolCategory from '../../../../components/ToolCategory.astro';

Tools that work at the clip level. `live_list_clips` reports both session slots
(including empty ones, so the model knows where it can create) and arrangement
clips, each with the id you pass to the note tools.

<ToolCategory names={['live_list_clips', 'live_create_midi_clip']} />
